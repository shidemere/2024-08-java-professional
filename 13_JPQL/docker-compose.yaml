version: '3.9'

services:
  homework_db:
    image: postgres:13.3
    container_name: postgresql_homework13
    environment:
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
      POSTGRES_DB: main
    ports:
      - "5432:5432"
